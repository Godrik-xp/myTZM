<root>
    <user uid="{{ user.id }}" username="{{ user.username }}" first_name="{{ user.first_name }}" last_name="{{ user.last_name }}"></user>
    <categories>
        {% include 'category.xml' %}
    </categories>
</root>